/**
 * ドメインモデル層。
 * エンティティ(ビジネスモデルを持つオブジェクト)を配置する。
 * このパッケージは他の層に一切依存してはならない。
 * フレームワーク（Spring）やDB（JPA）に関する記述、@Entity などのアノテーションを使用せず、純粋なJavaのクラスだけで構成する。
 */
package com.myapp.template.domain.model;